name: 'Add Issue to Factory Project'
description: 'Adds tracking issue to Factory Portfolio project'
inputs:
  issue_node_id:
    description: 'Issue node ID to add to project'
    required: true
  study_title:
    description: 'Study title for error messages'
    required: true
  factory_project_number:
    description: 'Factory Portfolio project number'
    required: true
  lead_name:
    description: 'Study lead name for custom field'
    required: true
  org_token:
    description: 'Organization admin token'
    required: true

runs:
  using: 'composite'
  steps:
    - name: Add to Factory Portfolio project
      uses: actions/github-script@v7
      with:
        github-token: ${{ inputs.org_token }}
        script: |
          const factoryNumber = parseInt("${{ inputs.factory_project_number }}", 10);
          const owner = "${{ github.repository_owner }}";
          const issueNodeId = "${{ inputs.issue_node_id }}";
          const studyTitle = "${{ inputs.study_title }}";
          const leadName = "${{ inputs.lead_name }}";
          
          console.log(`🔍 Adding issue to Factory Portfolio project #${factoryNumber}`);
          
          if (!factoryNumber) {
            console.log("⚠️ FACTORY_PROJECT_NUMBER not set in repository variables");
            console.log("To fix: Settings → Variables → Repository variables → Add 'FACTORY_PROJECT_NUMBER'");
            return;
          }
          
          try {
            // Query Factory project and custom fields
            const factoryResult = await github.graphql(`
              query($login: String!, $num: Int!) {
                user(login: $login) {
                  projectV2(number: $num) {
                    id
                    title
                    fields(first: 20) {
                      nodes {
                        ... on ProjectV2Field {
                          id
                          name
                        }
                        ... on ProjectV2SingleSelectField {
                          id
                          name
                        }
                      }
                    }
                  }
                }
              }
            `, { login: owner, num: factoryNumber });
            
            const project = factoryResult.user.projectV2;
            if (!project) {
              console.log(`❌ Factory project #${factoryNumber} not found`);
              return;
            }
            
            console.log(`📋 Found project: ${project.title}`);
            
            // Find Lead field
            const leadField = project.fields.nodes.find(field => field.name === 'Lead');
            if (!leadField) {
              console.log(`⚠️ 'Lead' custom field not found in project`);
            }
            
            // Add issue to project
            const addResult = await github.graphql(`
              mutation($projectId: ID!, $contentId: ID!) {
                addProjectV2ItemById(input: {
                  projectId: $projectId,
                  contentId: $contentId
                }) {
                  item { id }
                }
              }
            `, {
              projectId: project.id,
              contentId: issueNodeId
            });
            
            const itemId = addResult.addProjectV2ItemById.item.id;
            console.log(`✅ Added tracking issue to Factory Portfolio project`);
            
            // Set Lead field if it exists
            if (leadField) {
              try {
                await github.graphql(`
                  mutation($projectId: ID!, $itemId: ID!, $fieldId: ID!, $value: String!) {
                    updateProjectV2ItemFieldValue(input: {
                      projectId: $projectId,
                      itemId: $itemId,
                      fieldId: $fieldId,
                      value: {
                        text: $value
                      }
                    }) {
                      projectV2Item { id }
                    }
                  }
                `, {
                  projectId: project.id,
                  itemId: itemId,
                  fieldId: leadField.id,
                  value: leadName
                });
                console.log(`✅ Set Lead field to: ${leadName}`);
              } catch (fieldError) {
                console.log(`⚠️ Failed to set Lead field: ${fieldError.message}`);
              }
            }
            
            // Set initial Objective field to 'Analysis Package Prototype'
            const objectiveField = project.fields.nodes.find(field => field.name === 'Objective');
            if (objectiveField) {
              try {
                const objectiveOption = objectiveField.options.find(option => option.name === 'Analysis Package Prototype');
                if (objectiveOption) {
                  await github.graphql(`
                    mutation($projectId: ID!, $itemId: ID!, $fieldId: ID!, $optionId: String!) {
                      updateProjectV2ItemFieldValue(input: {
                        projectId: $projectId,
                        itemId: $itemId,
                        fieldId: $fieldId,
                        value: { singleSelectOptionId: $optionId }
                      }) {
                        projectV2Item { id }
                      }
                    }
                  `, {
                    projectId: project.id,
                    itemId: itemId,
                    fieldId: objectiveField.id,
                    optionId: objectiveOption.id
                  });
                  console.log(`✅ Set Objective field to: Analysis Package Prototype`);
                } else {
                  console.log(`⚠️ Objective option 'Analysis Package Prototype' not found`);
                }
              } catch (fieldError) {
                console.log(`⚠️ Failed to set Objective field: ${fieldError.message}`);
              }
            } else {
              console.log(`⚠️ Objective field not found in project`);
            }
            
          } catch (error) {
            console.log(`❌ Failed to add issue to project: ${error.message}`);
            console.log("🔧 MANUAL STEPS REQUIRED:");
            console.log(`1. Go to: https://github.com/users/${owner}/projects/${factoryNumber}`);
            console.log("2. Click '+ Add items'");
            console.log(`3. Search for: ${studyTitle}`);
          }